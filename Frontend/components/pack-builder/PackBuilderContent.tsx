// components/pack-builder/PackBuilderContent.tsx
"use client";
import { PackBuilder } from "./pack-builder";  // Assuming your original import

export function PackBuilderContent() {
  return <PackBuilder />;
}